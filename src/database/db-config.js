const config = {
    user:'sdohmen',
    password:'mysql',
    server: 'localhost',
    database: 'usuarios',
    options: {
        trustedConnection: false,
        eneableArithPort: true,
        encrypt: false,
    }
}

module.exports = config;